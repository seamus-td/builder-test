import { Component, Class } from '@builder.io/sdk';
export interface AngularComponent extends Component {
    tag: string;
}
export declare function BuilderBlock(options: AngularComponent): (component: Class) => Class;
